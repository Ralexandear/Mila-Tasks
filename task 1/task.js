const firstEvent = {
    date: new Date(2024, 5, 12), //дата события
    participantIds: [1, 2, 3, 4] //id участников события
}

const secondEvent = {
    date: new Date(2024, 8, 27), //дата события
    participantIds: [1, 2, 3, 4] //id участников события
}


/**
 * 
 * @param {typeof firstEvent} firstEvent 
 * @param {typeof secondEvent} secondEvent 
 */
function eventComparion(firstEvent, secondEvent) {
    /**
     * TODO
     * Написать функцию которая дает в консоль ответ какое из событий случится раньше     
     * И ответит на вопрос - одинаковые ли списки участников событий или нет
     */
}